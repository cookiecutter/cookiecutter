name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '*'

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        with:
          projectKey: jabegel_cookiecutter
          organization: jabegel
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
